# Audit — 22 September 2026

Audited commit `45eb8b5` (`main`) against a read-only snapshot. Three days to
the 25 Sept submission deadline.

**Method.** Ten parallel finders across distinct dimensions (contract, chain
hooks, pure logic, React/Next, security, documented invariants, a11y, tests,
dead code, performance) produced 77 raw findings. The adversarial verification
panel did not complete — it was killed twice by session limits — so **agent
verification was abandoned and the high-value findings were verified by hand
instead**, with executable probes where a probe was possible.

Everything below is backed by a passing test, an HTTP response, or a grep — not
by model opinion. Nothing is reported on an agent's say-so.

**Status.** Findings 1-8 are fixed in the pull request that carries this file,
each with a regression test that fails on the previous code. The remaining 18
leads have since been verified by hand too: 14 confirmed, 1 confirmed but
unreachable in production, 3 refuted. None of those 18 are fixed here — see
"Outstanding leads — now verified".

---

## Baseline (run against this commit)

| Check | Result |
|---|---|
| `npm run typecheck` | clean |
| `npm run lint` | clean |
| `npm test` | 39 passed / 39 |
| `npm run build` | succeeds |
| `npm run test:contracts` | **not run** — Foundry not installed on the audit machine |

---

## Confirmed — verified by hand

### 1. BLOCKER — `/pet` fabricates a pet and labels it `live`

`src/app/pet/pet-live-client.tsx:15` and `:95`

`PLACEHOLDER_PET` is a hand-written view model — `displayName: "Mochi"`,
`growthPoints: 0`, `nextStageAt: 20`, and critically `dataMode: "live"`. Line 95
does `registry.pet ?? PLACEHOLDER_PET` and passes the result to `CarePanel`,
which renders its growth line *unconditionally* for every action state
(`src/components/pet/CarePanel.tsx:165-169`).

So when a live read fails, the page renders both of these at once:

> "Pet data could not be loaded. **No preview data is shown.**"
> "**0 growth points. Next stage at 20.**"

The app asserts it is showing no preview data while showing fabricated preview
data labelled `live`. A disconnected visitor takes the same path and sees growth
stats for a pet named Mochi that does not exist.

This contradicts the project's headline invariant in three places at once:
`null`/unknown must never render as `0`; fixtures are never a fallback for a
failed read; and the README's "the UI never shows a number it cannot justify."

**Proof.** Rendering `CarePanel` with `PLACEHOLDER_PET` and the real
`resolveCareActionState({readStatus: "error", hasPet: false, …})` output asserts
both strings present simultaneously — test passes.

**Fix.** Delete `PLACEHOLDER_PET`. Render the no-pet / failed-read branch from
an explicit empty or error state that carries no numbers. If `CarePanel` needs a
pet-shaped prop, make the prop nullable and have the panel omit the growth line
when it is absent — do not synthesise a pet.

---

### 2. BLOCKER (environment-dependent) — unbounded RPC loop on any chain that is not X Layer

`src/lib/chains.ts:53`, consumed at `src/hooks/useCommunityStats.ts:47` and
`src/hooks/usePetRegistry.ts:93`

`chainFromDeployment` returns the module-level constants for chain ids `196` and
`1952`, but for **any other id** it calls `defineChain({...})` and returns a
brand-new object. Both hooks call it directly in the render body with no
`useMemo`, and put `chain` in their effect dependency array. Every render
produces a new `chain` identity → the effect re-runs → it calls `setCommunity` /
sets pet state → re-render → new `chain` → effect re-runs. Unbounded.

**Proof.** Mounting the real `useCommunityStats` once with a mocked transport:

| Chain id | `readContract` calls in 400 ms |
|---|---|
| 196 (X Layer mainnet) | **1** |
| 31337 (Anvil local) | **7,157** |

~18,000 RPC calls/second from a single mount.

**Blast radius.** X Layer mainnet (196) and testnet (1952) are hardcoded, so the
loop does **not** fire on the submission target — this is not a demo blocker if
you deploy to X Layer. It fires on **local Anvil (31337)**, which is the exact
environment every piece of QA evidence in `docs/qa/` was gathered in, and on any
custom RPC.

**Fix.** Memoise on the primitive fields, not the object:

```ts
const chain = useMemo(
  () => chainFromDeployment(deployment),
  [deployment.chainId, deployment.rpcUrl, deployment.networkName,
   deployment.currencySymbol, deployment.explorerBaseUrl],
);
```

in both hooks. (`useWallet.ts:93` has the same call but does not put it in a
dependency array, so it is not looping — memoise it anyway for consistency.)

---

### 3. HIGH — a failed read offers a care transaction that bypasses both guards

`src/lib/care-action-machine.ts:89`

`readStatus === "error"` is checked *before* `hasPet` and *before* the cooldown
branch, and it returns `{kind: "error"}`. `CarePanel` renders the `error` state
as an **enabled** "Try care again" button wired to `onCare`
(`CarePanel.tsx:139-148`), which calls `registry.care()` for real.

So when the read has failed — meaning the app does not know whether the user has
a pet or is in cooldown — it still offers to submit a care transaction.

**Proof.** With `readStatus: "error"`, `hasPet: false`, and a cooldown active
until 2099, the resolved state is `error`, the button is enabled, and clicking it
fires `onCare` exactly once — test passes.

The contract will revert (`NoPet` / `AlreadyCaredToday`), so no bad state is
written, but the user pays gas to discover a rule the UI already knew.

**Fix.** Either gate the retry so it re-reads rather than submitting a write, or
carry the last known pet/cooldown state through the error branch and disable care
when either guard is unsatisfied.

---

### 4. MEDIUM — a successful adopt is announced as a confirmed care

`src/lib/care-action-machine.ts:10`, `src/app/pet/pet-live-client.tsx:59`

`LivePetControllerInput` has no field identifying *which* write is in flight.
`pet-live-client` reads `registry.txKind` (line 41) and uses it for the community
refresh (line 51) but never passes it into `resolveCareActionState`. So an adopt
receipt and a care receipt are indistinguishable: both set `txPhase: "success"`,
which renders "Care is confirmed."

**Proof.** `resolveCareActionState` with the adopt flow's inputs returns
`{kind: "success"}` and `CarePanel` renders "Care is confirmed" — test passes.

**Fix.** Add `txKind` to `LivePetControllerInput`, pass `registry.txKind`, and
return a distinct state (or message) for a confirmed adoption.

---

### 5. MEDIUM — wallet errors render as ordinary body text

`src/app/globals.css:494`

`.pet-live-error { color: var(--danger); }` — `--danger` is **never defined**
anywhere in the codebase and there is no fallback, so the declaration is invalid
and the colour inherits. Every wallet error surfaced at
`pet-live-client.tsx:181-183` looks like normal paragraph text.

**Proof.** `--danger` appears exactly once in `src/`, as a `var()` consumer;
zero definitions.

**Fix.** Define the token alongside the other semantic colours (light and dark),
or point the rule at the existing error token the CSS modules already use.

---

### 6. HIGH — a care confirmed on chain is reported as a failure

`src/hooks/usePetRegistry.ts:278`

Confirmed while fixing the above. After `receipt.status === "success"` passes,
the post-receipt `petOf` re-read is **inside the same `try`**. If that read
throws — an RPC blip is enough — control falls to the catch, which sets
`txPhase: "error"` and the message *"Care failed. No progress was awarded."*

The care **did** succeed on chain. The user is told the opposite, and will
likely retry, which the contract then rejects with `AlreadyCaredToday`.

This is QA checklist item **I10** ("Confirmation succeeds but refresh fails →
Transaction truth and stale/unavailable display are distinguished") inverted.

**Fix.** Wrap the re-read in its own `try`/`catch`. On failure keep
`txPhase: "success"` — the write is confirmed — and mark the snapshot
`readStatus: "error"` with a message saying the action confirmed but the
refresh failed, so the display is flagged stale rather than false.

---

### 7. HIGH — a write that lands after an account switch is applied to the new wallet

`src/hooks/usePetRegistry.ts:293`

Confirmed while fixing the above. The read effect has a `cancelled` flag, but
`runWrite` has no equivalent. It captures `address` in its closure and, after
awaiting a receipt and a re-read, calls `setSnapshot(...)` unconditionally. If
the user switches accounts mid-transaction, wallet A's pet is written into
state while wallet B is connected.

This is QA checklist item **I8** ("Switch wallet or chain → Previous identity's
cached pet is not presented as current").

**Fix.** Track the identity cache key in a ref, capture it when the write
starts, and bail out after every await if it no longer matches.

---

### 8. LOW — README undercounts the test suite

`README.md:196` and `README.md:282` both claim **29 app tests**. The suite
contains **39** (verified by running it). A judge reading the README sees a
smaller number than the project has earned.

---

## Verified sound

Worth recording, because these are the claims most likely to be doubted:

- **`/dev/*` routes genuinely 404 in a production build.** Not assumed — built
  with `NODE_ENV=production`, served with `next start`, and requested over HTTP:
  `/dev/pet`, `/dev/landing`, `/dev/community` all return **404** with no fixture
  content in the body, while `/` and `/pet` return 200. The runtime `notFound()`
  gate in `src/app/dev/layout.tsx:11` holds. (QA checklist I12.)
- **The community panel's null-vs-zero discipline is real** in
  `map-community.ts` / `CommunityPanel.tsx` — unknown renders as unknown.
- **`DEPLOYMENT` contains no invented address**; committed status is
  `not-deployed` exactly as documented.
- **No token, approval, transfer, staking or payable path exists** anywhere in
  `contracts/` or `src/`.
- typecheck, lint and the production build are all clean.

---

## Outstanding leads — now verified

All 21 leads have been judged. Three (`usePetRegistry:293`, `usePetRegistry:313`,
`useCommunityStats:85`) became findings 6, 7 and the community-deps fix in this
pull request. The remaining 18 were checked by hand against `main`, with an
executable probe wherever a probe could settle the question.

A three-lens agent panel was attempted three times and died on session limits
each time; two lenses did complete (`celebrate-stuck`, `footer-contrast`) and
both agreed with the hand verdict below.

### Confirmed — real defects, not fixed here

| Lead | Location | Severity | What settles it |
|---|---|---|---|
| `src/hooks/**` has no tests at all | `src/hooks/` | high (coverage) | No `*.test.*` under `src/hooks` on `main`. This PR adds the first one. |
| Live pet home renders no `<h1>` | `src/app/pet/page.tsx:25` | medium | The configured branch returns `<PetLiveClient />` with no `h1`; the only `h1` (line 40) is in the *not-configured* branch. Since the X Layer deploy, the live branch is the production path. |
| Care-state transitions drop keyboard focus | `src/components/pet/CarePanel.tsx:170` | medium | Probe: focus the Care button, move to `awaiting-signature`, and `document.activeElement === document.body`. Every following state renders only `disabled` buttons, which cannot hold focus, so there is nothing to return to. |
| No error boundary anywhere in the App Router | `src/app/` | medium | No `error.tsx` or `global-error.tsx` exists. A render-time throw in the live client takes out the whole page. |
| `wallet_addEthereumChain` rejection escapes | `src/hooks/useWallet.ts:197` | medium | The `await` inside the `4902` branch has no `try`/`catch`, and the call site is `void wallet.switchNetwork()`. Declining "Add network" yields an unhandled rejection and no error state. |
| `Cared` event never asserted | `contracts/test/PetRegistry.t.sol:28` | medium | The file's only `vm.expectEmit` asserts `Adopted`. `Cared` (contract line 25) is never asserted, so its args can regress silently. |
| Footer contrast below AA | `src/app/globals.css:236` | medium | `--ink-faint` `#7a7189` on `--ground` `#f3edff` computes to **4.04:1** at 13.3px; AA needs 4.5:1. Dark mode is fine at 6.55:1, so this is light-scheme only. |
| CI installs forge-std unpinned | `.github/workflows/checks.yml:96` | low-med | `forge install foundry-rs/forge-std --no-git` with no tag or commit: CI tracks an upstream default branch. |
| No security response headers | `next.config.ts` | low-med | No `headers()` entry, so the wallet-signing pages carry no frame-ancestors, `X-Content-Type-Options` or `Referrer-Policy`. |
| Celebration never cleared | `src/hooks/usePetRegistry.ts:308` | low | Set true at 308; cleared only by the cacheKey reset (90) and the next write (224). `dismissTx` does not clear it, so the pulse persists after the transaction card is dismissed. |
| `submitting` state is unreachable | `src/lib/care-action-machine.ts:49` | low | Every `setTxPhase` call site writes idle/awaiting-signature/pending/success/error/rejected. Nothing ever writes `submitting`, so that branch is dead in the live app (previews still exercise it). |
| `localAnvil` is dead, and Anvil gets OKB | `src/lib/chains.ts:44` | low | `localAnvil` is exported and referenced nowhere; `chainFromDeployment` never returns it. The env override defaults `currencySymbol` to `"OKB"`, so a local Anvil chain is registered in the wallet as OKB. |
| No SPDX identifier on the contract | `contracts/src/PetRegistry.sol:1` | low | File opens with `pragma solidity`; the deploy script does carry `// SPDX-License-Identifier: MIT`. |
| Env override accepts `mainnet` | `src/lib/deployment.ts` | low | `readEnvOverride` accepts `statusRaw === "mainnet"`, so environment variables alone can point the build at a real-value chain with no separate gate. |

### Confirmed in CSS, unreachable in the live app

**`.artFrame` has no dark-scheme override** (`src/components/pet/pet.module.css:88`).
The frame paints a `rgb(255 255 255 / 70%)` radial highlight, and the dark block
at line 387 overrides only `.stageCurrent` and `.art`. Compositing that blob over
the dark `--action-wash` gives `rgb(192,189,202)`, against `--ink-soft` `#c3b8d6`
placeholder text: **1.02:1** — effectively invisible.

But the placeholder only renders when `artSrc` is `null`, and `map-pet.ts:47`
always assigns from `STAGE_ART`, which has all three stages backed by real PNGs
in `public/pets/`. So this is unreachable in production and shows up only in the
`/dev/*` previews — and in **QA checklist item U3**, which asks a tester to check
the missing-art state. A tester doing U3 in dark mode would see nothing.

### Refuted — do not re-report

- **`nextUtcDayStartIso` RangeError** (`src/lib/care-cooldown.ts:23`). Reaching the
  throw needs a `lastCareDay` far beyond anything `block.timestamp / 1 days` can
  produce. Not reachable against the deployed registry.
- **`txPhase === "success"` checked before `readStatus === "error"`**
  (`src/lib/care-action-machine.ts:78`). On `main` the combination barely arises,
  because a failed re-read set `txPhase: "error"`. After finding 6 in this PR the
  ordering is deliberate: the transaction really did confirm, so `success` is the
  honest answer, and the stale display is flagged through `readErrorMessage`.
- **RPC URL not verified against the chain id** (`src/lib/chains.ts:62`). Real
  hardening, not a defect: the wallet-versus-config mismatch is already caught by
  the `wrongChain` check, and a wrong `rpcUrl` is deployer misconfiguration rather
  than a code path the app can take by itself.

---

## What this pull request changes

Findings 1-8, each with a regression test that fails on the previous code:

| # | Fix | Files |
|---|---|---|
| 1 | `PLACEHOLDER_PET` deleted; `CarePanelProps.pet` is now nullable and the panel omits growth numbers when no pet is known | `pet-live-client.tsx`, `CarePanel.tsx`, `view-models.ts` |
| 2 | `getActiveDeployment()` is resolved once, and `chain` is memoised in both hooks | `deployment.ts`, `usePetRegistry.ts`, `useCommunityStats.ts` |
| 3 | A failed read maps to `unavailable`, not `error`, so no care write is offered | `care-action-machine.ts` |
| 4 | `txKind` added to the machine input; an adopt receipt no longer reads as a confirmed care | `care-action-machine.ts`, `pet-live-client.tsx` |
| 5 | `.pet-live-error` uses the existing `--alert` token instead of the undefined `--danger` | `globals.css` |
| 6 | The post-receipt re-read has its own catch: a confirmed write stays confirmed and the display is marked stale | `usePetRegistry.ts` |
| 7 | The write path bails out after every await if the wallet/chain identity changed | `usePetRegistry.ts` |
| 8 | README test count corrected to 39 | `README.md` |

Finding 2's fix would have **exposed** a latent bug: `useCommunityStats` was
missing `address` from its effect deps, and the identity churn was the only
thing re-running that effect. With the churn gone, the counter would have stuck
on "Reading…" after every connect or account switch on X Layer. The dep is
added in the same change.

Gate after the change: typecheck clean, lint clean, 47/47 tests, production
build succeeds, and `/dev/*` still returns 404 in the production build.

## Recommended next

1. The confirmed leads above are now triaged, not open questions. Before the
   deadline, only three are worth the risk: the missing `<h1>` and the keyboard
   focus loss (both small, both user-visible, both in the demo path), and the
   `wallet_addEthereumChain` rejection, which is silent failure on a wallet
   action a judge may well perform. Everything else — SPDX, CI pinning, security
   headers, the dead `submitting` state, `localAnvil`, the OKB label — is
   post-submission cleanup and should not compete with the demo video.
2. The X Layer deployment remains the critical path (`docs/STATUS.md`); none of
   this changes that.
3. `src/hooks/**` now has one test file. It had none, which is why findings 1,
   3, 4, 6 and 7 were all invisible: nothing rendered those states.