# OKX submission preparation — observed evidence

Task **OKX-PREP**, 22 September 2026. This record distinguishes real browser
actions, read-only RPC/HTTP checks, mocked tests and unperformed wallet actions.
**No browser wallet action passed. No adoption or care was signed.**

## Branch provenance

- Starting `origin/main`: `a3c66ab`.
- `feat/public-pet-page`: `57a507f`, already merged through PR #21
  (`7c7a3f8`), with badge correction #27 also on main. Not cherry-picked again.
- `fix/live-pet-truthfulness-and-read-loop`: `30c2dab`, four unmerged commits
  brought into local `prep/okx-dev-day-submission`.
- Integration commit: `13298d9`. Preserved the public share import/component
  while deleting `PLACEHOLDER_PET`. No conflict markers remain.
- Added guards against rejected adoption offering care, stale async results
  across wallet sessions (including A → B → A), old operations unlocking newer
  ones, and background reads changing progress during an unconfirmed write.
  Cooldown uses the chain block timestamp with a read pinned to that block;
  confirmed reads refresh every 30 seconds.
- Strengthened the community account-switch regression to rerender the same
  component and key rather than remounting it.
- Independent source review found no additional actionable regression. This
  is not a security audit and not browser-wallet evidence.
- Claude's separate worktree and its unfinished merge were left untouched.
- This commit is local integration, **not a production rollout**. Existing
  production observations below concern the currently deployed site.

### Published review branch and preview follow-up

The integration and recording pack were pushed to
`prep/okx-dev-day-submission` and published as **draft
[PR #28](https://github.com/Chi944/memepet/pull/28)**. GitHub reported it
mergeable. Neither main nor the existing production domain was updated.

The repository's existing Vercel integration automatically created a preview
for commit `8a88f4bd02377ddbc3dbf0900b42523e5a98a041`:
https://memepet-21qy9t1rs-chi944s-projects.vercel.app.
Real Chrome verification on that exact preview observed:

- `/pet`: `Your pet` h1, no fabricated growth, community cares 0.
- Clicking Connect wallet: **No injected wallet was found**. Still BLOCKED,
  zero wallet passes; [unaltered screenshot](okx-prep-preview-wallet-blocked.png).
- Captured browser error logs on that `/pet` visit: empty.
- `/dev/pet`, `/dev/landing`, `/dev/community`: rendered the 404 page.

This verifies the final integrated **preview** without a wallet. It does not
prove signed flows or logged-out preview access, and it does not change the
local production-start rejection recorded below.

GitHub [CI run 35688426933](https://github.com/Chi944/memepet/actions/runs/35688426933)
completed successfully at that same commit. App and contract jobs passed,
including clean dependency installation and the production `/dev/*` gate
smoke step. Local startup was blocked, but this separate CI smoke actually
ran and passed. Subsequent documentation-only commits do not change the
verified app source; identify the final recording revision again before use.

## Actual browser actions

Used the user's real Chrome profile through the browser automation extension.
No provider mock, fake wallet, CLI adoption or CLI care was substituted.

| Surface / action | Observed outcome | Evidence classification |
|---|---|---|
| Open `https://memepet.vercel.app/pet`, click top Connect wallet | Wallet **Not installed**, chain **Unknown**, pet **None yet**; **No injected wallet was found.** No wallet prompt appeared | Attempted row 1; BLOCKED before approval |
| Same live page | Community cares 0; old production panel still showed `0 growth points. Next stage at 20.` with no pet | Production truthfulness defect; local fix not deployed |
| Open integrated `http://127.0.0.1:3300/pet` against Anvil, click Connect wallet | Same missing-provider error; `Your pet` h1 present; no fabricated growth line | Attempted local row 1; BLOCKED before approval; wallet-free rendering observed |
| Open live `/pet/0x2ec8471290793FeB64792861Ce3102d291ce1CA1` | Read-only page says **This wallet has not adopted a pet.** No fictional pet shown | Public read, not connection/adoption evidence |
| Read official submission form in existing Chrome session | Page 1 of 3 visible; required fields and deadline confirmed; no values entered or submitted | Requirements inspection only |

Screenshots (unaltered browser captures):

- [Live connection blocker](okx-prep-live-wallet-blocked.png).
- [Local integrated connection blocker](okx-prep-local-wallet-blocked.png).
- [Live public no-pet state](okx-prep-live-public-no-pet.png).

Full-page capture timed out twice on separate pages; viewport captures
succeeded. Local screenshot was captured before the final hook guard edits,
so it proves the visible integrated empty state, not final wallet behavior.

Chrome's extensions page could not be inspected: browser approval policy
blocked the URL and explicitly prohibited alternate routes around that block.
No workaround was attempted. The app's observed fact is **no injected
provider**, not a verified inventory of installed extensions.

## Wallet worksheet outcome

[BROWSER_WALKTHROUGH.md](../BROWSER_WALKTHROUGH.md) contains separate local and
live result records. Row 1 was attempted but could not reach approval. Rows
2–9 were **NOT RUN**, not passing. In particular:

- No actual rejection or signature confirmation.
- No adopt/care transaction hash or receipt.
- No confirmed pet refresh, cooldown, account switch or before/after total.
- Anvil day advancement was not executed in this walkthrough. Automated
  chain-time regression coverage does not substitute for that browser step.
- X Layer cannot use Anvil time travel; next-day live care remains unverified.

The user-provided public address was sufficient for read-only checks, not
signing. Human setup/unlock of an injected test wallet remains required.

## Read-only live checks

Public RPC: `https://testrpc.xlayer.tech/terigon`. Registry:
`0xe844152262D243a7B90F6e07FF7A67F1d7FeD216`.

Actual commands (Foundry binaries were invoked from
`C:\Users\User\.foundry\bin`):

```text
cast chain-id --rpc-url https://testrpc.xlayer.tech/terigon
cast call 0xe844152262D243a7B90F6e07FF7A67F1d7FeD216 "petOf(address)(bool,uint32,uint64,uint64)" 0x2ec8471290793FeB64792861Ce3102d291ce1CA1 --rpc-url https://testrpc.xlayer.tech/terigon
cast call 0xe844152262D243a7B90F6e07FF7A67F1d7FeD216 "communityStats(uint32)(uint256)" 1 --rpc-url https://testrpc.xlayer.tech/terigon
cast balance 0x2ec8471290793FeB64792861Ce3102d291ce1CA1 --rpc-url https://testrpc.xlayer.tech/terigon
cast code 0xe844152262D243a7B90F6e07FF7A67F1d7FeD216 --rpc-url https://testrpc.xlayer.tech/terigon
forge inspect --root contracts PetRegistry deployedBytecode
gh repo view Chi944/memepet --json visibility,defaultBranchRef,url
```

| Check | Actual result |
|---|---|
| Chain id | 1952 |
| Supplied wallet's `petOf` | false, 0, 0, 0 |
| `communityStats(1)` | 0 care actions; **does not establish zero adoptions** |
| Supplied wallet balance | 199985802959290148 wei = 0.199985802959290148 testnet OKB; not a gas estimate or proof of wallet control |
| Runtime code comparison | Exact hex-string match against compiled repository source; 2796 hex characters including `0x` |
| Repository | PUBLIC, default branch main |

`Invoke-WebRequest -SkipHttpErrorCheck` checked each deployed route:

| Route | Actual HTTP status |
|---|---:|
| `/` | 200 |
| `/pet` | 200 |
| `/dev/pet` | 404 |
| `/dev/landing` | 404 |
| `/dev/community` | 404 |
| `/dev/profile` | 404 |
| `/pet/not-an-address` | 404 |

These deployed-route results do not validate an unpublished local build.
No fresh clean-incognito console-error or mobile-viewport check was performed
in this session; earlier evidence remains separately dated.

## Local Anvil preparation actually performed

```text
anvil --host 127.0.0.1 --port 8545 --silent
cast chain-id --rpc-url http://127.0.0.1:8545
cast rpc eth_accounts --rpc-url http://127.0.0.1:8545
forge create --root contracts src/PetRegistry.sol:PetRegistry --rpc-url http://127.0.0.1:8545 --broadcast --unlocked --from 0xf39fd6e51aad88f6f4ce6ab8827279cfffb92266
npm run dev -- --hostname 127.0.0.1 --port 3300
```

- Chain id: 31337.
- Local-only contract: `0x5FbDB2315678afecb367f032d93F642f64180aa3`.
- Local deployment tx:
  `0x990e6bb46992369f8ed58abd02f040e48c98db8dec795a28a63cc90fbecccde6`.
- This is a local contract deployment via an unlocked Anvil account, **not** a
  browser wallet action, testnet deployment, adoption or care.
- All seven public deployment environment values were set in the dev-server
  process to local Anvil values. The existing `.env.local` was neither read
  nor edited. No secrets or signing keys were obtained or logged.
- The local dev server was stopped before the release build, and the Anvil
  process was stopped after evidence collection. Anvil is an ephemeral local
  test node; restarting creates a new chain. The local URL is no longer served.

## Automated checks — final integrated source

Node 24.19.0, npm 11.19.0. No dependency or lockfile change; `npm ci` was not
rerun. Final build/typecheck ran after implementation edits were frozen.

| Actual command | Result |
|---|---|
| `npm run lint` | PASS, exit 0 |
| `npm test` | PASS, 15 files / 73 tests, exit 0 |
| `npm run build` | PASS, exit 0 |
| `npm run typecheck` | PASS, exit 0 |
| `npm run test:contracts` with Foundry on process PATH | PASS, 13/13, exit 0 |
| `git diff --check` | PASS; Windows line-ending notices only |
| Final local production `npm run start -- --hostname 127.0.0.1 --port 3300` | BLOCKED by automatic command approval review before execution (`blocked by policy`); final local production HTTP smoke NOT RUN |

The build process explicitly set all seven public deployment values to the
committed X Layer testnet configuration, rather than relying on unknown local
overrides. It did not deploy the app. A background Anvil launcher was also
rejected earlier; a foreground silent local Anvil tool session was permitted.

Vitest emitted its existing warning about the future native config loader
and an environment-performance suggestion. No checks or rules were disabled.
Hook tests use controlled wallet/RPC mocks and cannot pass any manual row.

## Resume before recording or submission

1. Make a usable injected test wallet available in Chrome and unlock privately.
2. Review and publish the integrated app, then record its commit/deployment URL.
3. Run local rows 1–9 and applicable X Layer rows independently. Save real
   receipt hashes and before/after values. Preserve explicit NOT RUN entries.
4. Use the genuine successful QA screen capture in the demo, or a fresh wallet
   for adoption. A wallet that has adopted cannot repeat that step; same-day
   care is unavailable after its first success.
5. Complete remaining form-only requirements and record/upload the three
   speakers' clips. Do not publish conditional success narration without the
   matching evidence. Follow the [recording pack](../../demo/DEMO_SCRIPT.md).
